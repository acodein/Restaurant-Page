export const para = document.createElement("div");
para.id = "menu";
para.innerText = "This is menu tab";

const img  = document.createElement("img");
img.src = "calculator-thumbnail.png"

para.appendChild(img);